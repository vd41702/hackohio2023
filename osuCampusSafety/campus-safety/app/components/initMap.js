
const initMap = () => {
    // const map = new google.maps.Map(document.getElementById('map'), {
    //     center: { lat: 40.000317218522056, lng: -83.01565528751004 },
    //     zoom: 15,
    //     });

    //     const temp = crimeLatLongs.map(crime => {
    //         let latlong = crime["latLong"]
    //         const [lat, long] = latlong.split(',')
    //         return {lat:parseFloat(lat), long:parseFloat(long), count:crime["count"]}
    //     })

    //     // Prepare data for the heatmap
    //     const heatmapData = temp.map((crime) => {
    //         return { location: new google.maps.LatLng(crime.lat, crime.long), weight: crime["count"] }    
    //     })  

    //     // Create the heatmap layer
    //     const heatmap = new google.maps.visualization.HeatmapLayer({
    //     data: heatmapData,
    //     });

    //     heatmap.setMap(map)
    console.log("tests")
}

